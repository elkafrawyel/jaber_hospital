import 'package:flutter/material.dart';

class InProgressAppointments extends StatelessWidget {
  const InProgressAppointments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
