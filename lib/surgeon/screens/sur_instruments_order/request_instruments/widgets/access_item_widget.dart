import 'package:flutter/material.dart';

class AccessItemWidget extends StatelessWidget {
  const AccessItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [

      ],
    );
  }
}
